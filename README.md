# Full Stack Project - Angular + Spring Boot + MySQL

## 🚀 Overview
A full stack web application built with:
- **Backend:** Spring Boot (Java)
- **Frontend:** Angular
- **Database:** MySQL

## ⚙️ Backend Setup
1. Open the project in IntelliJ
2. Configure `application.properties` with your MySQL credentials
3. Run the Spring Boot application

## 💻 Frontend Setup
1. Open `frontend` in Visual Studio Code
2. Run `npm install`
3. Run `ng serve`

The frontend will be available at `http://localhost:4200` and the backend at `http://localhost:8080`.

## 📦 Database
MySQL database with tables: users, roles, etc.
